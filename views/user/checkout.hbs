<body>




    <!-- Start Banner Area -->
    <section class="banner-area organic-breadcrumb">
        <div class="container">
            <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
                <div class="col-first">
                    <h1>Checkout</h1>
                    <nav class="d-flex align-items-center">
                        <a href="index.html">Home<span class="lnr lnr-arrow-right"></span></a>
                        <a href="single-product.html">Checkout</a>
                    </nav>
                </div>
            </div>
        </div>
    </section>
    <!-- End Banner Area -->

    <!--================Checkout Area =================-->
    <section class="checkout_area section_gap">
        <div class="container">
            <div class="cupon_area">
                <input type="text" id="couponCode" value="" placeholder="Enter coupon code">
                <button class="tp_btn" onclick="applyCoupon()">Apply Coupon</button>
            </div>
            <div class="billing_details">
                <form class="row contact_form" action="" method="" id="checkout-form">
                    <div class="row">
                        <div class="col-lg-8">
                            <h3>Billing Details</h3>

                            <div class="col-md-6 form-group p_star">
                                <input type="text" class="form-control" id="name" value="" name="Name"
                                    placeholder="Name">

                            </div>
                            <div class="col-md-6 form-group p_star">
                                <input type="number" class="form-control" id="phone" value="" name="Phone"
                                    placeholder="Phone Number">

                            </div>
                            <div class="col-md-6 form-group p_star">
                                <input type="text" class="form-control" id="email" value="{{userDetails.email}}"
                                    name="Email" placeholder="Email">

                            </div>
                            <div class="col-md-12 form-group p_star">
                                <input type="text" class="form-control" id="building_Name" value="" name="Buidling_Name"
                                    placeholder="Buidling Name">

                            </div>
                            <div class="col-md-12 form-group p_star">
                                <input type="text" class="form-control" id="street_name" value="" name="Street_Name"
                                    placeholder="Street Name">

                            </div>
                            <div class="col-md-12 form-group p_star">
                                <input type="text" class="form-control" id="city" value="" name="City"
                                    placeholder="City">

                            </div>
                            <div class="col-md-12 form-group p_star">
                                <input type="text" class="form-control" id="district" value="" name="District"
                                    placeholder="District">

                            </div>
                            <div class="col-md-12 form-group p_star">
                                <input type="text" class="form-control" id="state" value="" name="State"
                                    placeholder="State">

                            </div>
                            <div class="col-md-12 form-group">
                                <input type="number" class="form-control" id="pincode" value="" name="Pincode"
                                    placeholder="Pincode">
                            </div>
                            <div>

                                <input type="text" id="userId" name="userId" value="{{userDetails._id}}" hidden>
                            </div>
                            <div>

                                <input type="text" id="addressId" name="addressId" value="" hidden>
                            </div>
                            <div>

                                <input type="text" id="couponNamess" name="couponName" value="" hidden>
                            </div>
                            <div>

                                <input type="text" id="grandTotal" name="grandTotal" value="{{totalValue.total}}"
                                    hidden>
                            </div>
                            <div>

                                <input type="text" id="Discount" name="Discount" value="" hidden>
                            </div>
                        </div>
                        <div class="col-lg-4">
                            <div class="order_box">

                                <h2>Your Order</h2>

                                <ul class="list">
                                    <li><a href="#">Product <span>Total</span></a></li>
                                    {{#each products}}
                                    <li><a href="#"> <span class="middle">{{this.product.Brand}} x
                                                {{this.quantity}}</span>
                                            <span class="last">Rs.{{this.proTotal}}</span></a></li>
                                    {{/each }}
                                </ul>
                                <ul class="list list_2">
                                    <li><a href="#"> Coupon applied<span id="couponNames">Nil</span></a></li>
                                    <li><a href="#"> Discount <span id="discount">0</span></a></li>
                                    <li><a href="#">Sub Total <span id="subTotal">{{totalValue.total}}</span></a></li>
                                    <li><a href="#">Grand Total <span id="GrandTotal">{{totalValue.total}}</span></a>
                                    </li>

                                </ul>
                                <div class="payment_item">
                                    <div class="radion_btn">
                                        <input type="radio" id="f-option5" name="Payment-method" value="COD" required>
                                        <label for="f-option5">Cash On Delivery</label>
                                        <div class="check"></div>
                                    </div>
                                </div>
                                <div class="payment_item active">
                                    <div class="radion_btn">
                                        <input type="radio" id="f-option6" name="Payment-method" value="ONLINE">
                                        <label for="f-option6">Paypal </label>
                                        <img src="img/product/card.jpg" alt="">
                                        <div class="check"></div>
                                    </div>

                                </div>
                                <button class="primary-btn" onclick="couponUse('{{userDetails._id}}')"
                                    type="submit">Confirm </button>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-lg-8">
                <div class="row">
                    {{#each userAddress}}
                    <div class="col-md-4">
                        <div class="card mb-4 mb-md-0">
                            <div class="card-body">
                                <p class="mb-4"><span class="font-weight-bold">name :</span> {{this.name}}
                                </p>
                                <p class="mb-4"><span class="font-weight-bold">phone :</span> {{this.phone}}
                                </p>
                                {{!-- <p class="mb-4"><span class="font-weight-bold">email:</span>
                                    {{../userDetails.email}}
                                </p> --}}
                                <p class="mb-4">
                                    <span class="font-weight-bold">Address :</span>
                                    {{this.building_Name}},{{this.street_name}},{{this.state}}
                                </p>

                                <p class="mb-4"><span class="font-weight-bold">pincode :</span> {{this.pincode}}</p>
                                <div>
                                    <button onclick="deliveryAddress('{{../userDetails._id}}','{{this.addId}}')"
                                        data-toggle="modal" data-target="#editAddress" class="btn btn-info">Add
                                        Address</button>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

        </div>
    </section>
    <!--================End Checkout Area =================-->
    <script>
        function deliveryAddress(userId, addressId) {
            $.ajax({
                url: '/deliveryAddress',
                data: {
                    user: userId,
                    addressId: addressId
                },
                method: 'post',
                success: (response) => {
                    Swal.fire("currentAddress ")
                    document.getElementById('name').value = response.name
                    document.getElementById('phone').value = response.phone
                    document.getElementById('building_Name').value = response.building_Name
                    document.getElementById('street_name').value = response.street_name
                    document.getElementById('city').value = response.city
                    document.getElementById('district').value = response.district
                    document.getElementById('state').value = response.state
                    document.getElementById('pincode').value = response.pincode
                    document.getElementById('addressId').value = response.addId
                }
            })
        }

        $("#checkout-form").submit((e) => {
            e.preventDefault()
            $.ajax({
                url: '/place-order',
                method: 'post',
                data: $('#checkout-form').serialize(),
                success: ((response) => {
                    Swal.fire(response)
                    if (response.codSuccess) {
                        location.href = '/orderPlaced'
                    } else {
                        rezorpayPayment(response)
                    }
                })
            })
        })

        function rezorpayPayment(order) {
            var options = {
                "key": "rzp_test_9MA0VfBXpxI6iH", // Enter the Key ID generated from the Dashboard
                "amount": order.amount, // Amount is in currency subunits. Default currency is INR. Hence, 50000 refers to 50000 paise
                "currency": "INR",
                "name": "Footlovers",
                "description": "Test Transaction",
                "image": "https://example.com/your_logo",
                "order_id": order.id, //This is a sample Order ID. Pass the `id` obtained in the response of Step 1
                "handler": function (response) {

                    verifyPayment(response, order)
                },

                "prefill": {
                    "name": "Gaurav Kumar",
                    "email": "gaurav.kumar@example.com",
                    "contact": "9999999999"
                },

                "notes": {
                    "address": "Razorpay Corporate Office"
                },
                "theme": {
                    "color": "#3399cc"
                }
            };
            var rzp1 = new Razorpay(options);
            rzp1.open();
        }

        function verifyPayment(payment, order) {
            $.ajax({
                url: '/verify-payment',
                data: {
                    payment,
                    order
                },
                method: 'post',
                success: (response) => {
                    if (response.status) {
                        location.href = '/orderPlaced'
                    } else {
                        Swal.fire("Payment failed")
                    }
                }
            })

        }

        function applyCoupon() {
            var couponCode = document.getElementById('couponCode').value
            var userId = document.getElementById('userId').value
            var subtotal = document.getElementById('subTotal').innerHTML
            $.ajax({
                url: '/applyCoupon',
                method: 'post',
                data: {
                    coupon: couponCode,
                    userId: userId,
                    subtotal: subtotal
                },
                success: (response) => {
                    if (response.expired) {
                        Swal.fire('sorry coupon expired')
                    } else if (response.used) {
                        Swal.fire('failed','already used','attempt failed')
                    } else if (response.notAvailable) {
                        console.log(response, "invalid")
                        Swal.fire('failed','invalid coupon')
                    } else if (response.orderLimit) {
                        Swal.fire("minimum cart value "+response.minCartValue)
                    } else {
                        var discount = response.reduction
                        console.log(discount, "ttttprrrrptttt")
                        var GrandTotal = subtotal - discount
                        console.log(discount, GrandTotal, "tttttttttttt")
                        document.getElementById('discount').innerHTML = discount
                        document.getElementById('GrandTotal').innerHTML = GrandTotal
                        document.getElementById('couponNamess').value = response.coupon
                        document.getElementById('couponNames').innerHTML = response.coupon
                        document.getElementById('grandTotal').value = GrandTotal
                        document.getElementById('Discount').value = discount
                        Swal.fire('Hurray', 'Coupon Applied','success')
                        console.log(response, "ttttrrrr")
                    }
                }
            })
        }


    </script>

</body>

</html>